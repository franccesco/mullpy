language: python
python:
  - '3.4'
  - '3.5'
  - '3.6'
  - '3.7'
before_install:
  - pip install poetry
install:
  - poetry install
script:
  - poetry run coverage run -m pytest
after_success:
  - poetry run mullpy
after_script:
  - poetry run coveralls
